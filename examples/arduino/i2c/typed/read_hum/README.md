# Arduino I2C Read HUM

This sketch performs one typed humidity read and first queries the active output mask.

## Use It

- Set `HUM_I2C_ADDRESS`.
- Upload the sketch and open Serial Monitor at `115200`.
- Move to `../../advanced/hum_workflow/hum_workflow.ino` if you need output selection or temperature-calibration state.
