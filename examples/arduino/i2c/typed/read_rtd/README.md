# Arduino I2C Read RTD

This sketch performs one typed RTD read and first queries the active temperature scale.

## Use It

- Set `RTD_I2C_ADDRESS`.
- Upload the sketch and open Serial Monitor at `115200`.
- Move to `../../advanced/rtd_workflow/rtd_workflow.ino` if you need logger, memory, or calibration state.
