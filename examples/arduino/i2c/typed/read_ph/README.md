# Arduino I2C Read pH

This sketch performs one typed pH read over Arduino I2C.

## Use It

- Set `PH_I2C_ADDRESS`.
- Upload the sketch and open Serial Monitor at `115200`.
- Use it after `inspect_device` when you want the smallest pH read path without calibration or workflow state.
